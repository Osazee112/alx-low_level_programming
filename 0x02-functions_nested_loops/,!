#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 * description : function to only accpt absolute integer
 * Return : Alway zero on success
 */

int _abs(int n)
{
	if ( n >= 0)
	{
	return(n);
	}
	else
		return(0);
}
